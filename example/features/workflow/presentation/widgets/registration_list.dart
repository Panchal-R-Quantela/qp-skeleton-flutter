import 'package:flutter/material.dart';

class RegistrationListPage extends StatelessWidget {
  const RegistrationListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text("RegistrationList")),
        body: Container(child: Text("Hey")));
  }
}
